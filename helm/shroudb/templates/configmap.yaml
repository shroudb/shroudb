apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "shroudb.fullname" . }}-config
  labels:
    {{- include "shroudb.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- .Values.config | nindent 4 }}
