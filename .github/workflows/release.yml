name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: shroudb/.github/.github/workflows/rust-release.yml@main
    permissions:
      contents: write
      packages: write
    with:
      product: "shroudb"
      server_package: "shroudb"
      server_binary: "shroudb"
      cli_package: "shroudb-cli"
      cli_binary: "shroudb-cli"
      docker_server_target: "shroudb"
      docker_cli_target: "shroudb-cli"
      crates: "shroudb-protocol,shroudb-client,shroudb,shroudb-cli"
    secrets:
      REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}
      GH_PAT: ${{ secrets.GH_PAT }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
